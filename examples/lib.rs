// use clap::Parser;
// use halo2_scaffold::scaffold::cmd::Cli;

pub mod binary_counting;
pub mod ipa_rust_simple_hash_batch;
fn main() {
    env_logger::init();
    // let args = Cli::parse();
    // run(compute_binary_counting, args);
    // MockProver::run(100, binary_counting,  input);
    println!("Hello World");
}
